#!/usr/bin/env node
import fs from "node:fs"; import path from "node:path"; import { git,hooksDisabled,normalizeInput,projectIdOf,projectRootOf,readStdin,stateRoot } from "./lib.mjs"; async function main(){const {raw}=await readStdin();if(hooksDisabled("session:end"))return;const i=normalizeInput(raw),root=projectRootOf(i.cwd),id=projectIdOf(root,!!git(root,["rev-parse","--show-toplevel"])),dir=path.join(stateRoot(),"sessions",id);fs.mkdirSync(dir,{recursive:true});fs.writeFileSync(path.join(dir,`hook-${i.sessionId}.json`),JSON.stringify({saved_at:new Date().toISOString(),topic:"Hook-captured session",next_step:"Review git status and continue the active task.",transcript_path:i.transcriptPath},null,2));}main().catch(()=>process.exit(0));
