import { DECISION_STATUSES, DECISIONS_RELATIVE_DIR, listDecisions, recordDecision } from "../core/decision-ledger.mjs";
export function createDecisionTools(host) { const projectRootFor = async ({ project_path, task_id }) => { if (task_id) { const record = await host.taskStore.read(task_id); return { projectRoot: (await host.resolveProjectIdentity(record.project.path)).project_root, record }; } if (!project_path) throw new Error("project_path or task_id is required."); return { projectRoot: (await host.resolveProjectIdentity(project_path)).project_root, record: null }; }; return { definitions: [{ name: "record_decision", description: "Record a numbered ADR under .ai-dev/decisions.", inputSchema: { type: "object", properties: { project_path: { type: "string" }, task_id: { type: "string" }, title: { type: "string" }, context: { type: "string" }, decision: { type: "string" }, alternatives: { type: "array", items: { type: "string" } }, consequences: { type: "array", items: { type: "string" } }, tags: { type: "array", items: { type: "string" } }, status: { type: "string", enum: DECISION_STATUSES }, supersedes: { type: "string" } }, required: ["title", "decision"] } }, { name: "list_decisions", description: "List ADRs newest first.", inputSchema: { type: "object", properties: { project_path: { type: "string" }, task_id: { type: "string" }, status: { type: "string", enum: DECISION_STATUSES }, tag: { type: "string" }, limit: { type: "number" } } } }], handlers: { async record_decision(args) { const { projectRoot, record } = await projectRootFor(args), result = await recordDecision(projectRoot, { ...args, task_id: args.task_id || "" }); const checkpoint = record && record.status !== "complete" ? await host.taskStore.checkpoint(record.id, { summary: `Decision recorded: ${result.record.id} ${result.record.title}`, changedFiles: [result.path], notes: result.record.decision }).then((x) => ({ task_id: x.id, checkpoints: x.checkpoints.length })) : null; host.markSearchIndexDirty?.(`decision recorded: ${result.path}`); return { action: "decision_recorded", project_path: projectRoot, decision: result.record, path: result.path, superseded: result.superseded, checkpoint, next_step: `Commit ${DECISIONS_RELATIVE_DIR} with the code change so the decision travels with the repository.` }; }, async list_decisions(args) { const { projectRoot } = await projectRootFor(args), decisions = await listDecisions(projectRoot, args); return { project_path: projectRoot, directory: DECISIONS_RELATIVE_DIR, count: decisions.length, decisions }; } }, readOnly: ["list_decisions"] }; }
