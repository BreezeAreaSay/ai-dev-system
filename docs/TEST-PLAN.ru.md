# Приёмочное тестирование 2.1.0 — где что лежит

ТЗ рассчитано на человека, у которого **не установлено ничего** и который про MCP слышит
впервые. Начинается с установки Node и git, заканчивается работающей задачей в ИИ-клиенте.

Документ разделён по операционным системам: тестировщику выдаётся один файл целиком, без веток
«а на Windows иначе».

| Документ | Кому | Когда брать |
| --- | --- | --- |
| [TEST-PLAN.macos.ru.md](TEST-PLAN.macos.ru.md) | macOS 13+, отдельно Apple Silicon и Intel | новая машина |
| [TEST-PLAN.windows.ru.md](TEST-PLAN.windows.ru.md) | Windows 11, отдельно x64 и ARM64 | новая машина, **и повторный прогон после 16.09** |
| [TEST-PLAN.linux.ru.md](TEST-PLAN.linux.ru.md) | Ubuntu 22.04 / 24.04 и родственные на glibc, x64 и ARM64 | новая машина |
| [RETEST.ru.md](RETEST.ru.md) | тот, кто уже прогонял раньше | короткая проверка изменённого, 10–20 минут |

**Какой документ кому.** Человеку, который эту систему ещё не ставил, — полный документ его
операционной системы. Тому, кто уже прогонял 15–16.09, — `RETEST.ru.md`: там только то, что
менялось, и повторять три часа незачем. Исключение — Windows: там основной раздел не завершался,
и его нужно пройти целиком заново.

## Что внутри каждого

| Раздел | Про что |
| --- | --- |
| 0 | Что такое MCP и что от тестировщика нужно — на языке человека, который этого не знает |
| 1 | Что поставить на машину и как именно (Node, git, PowerShell 7 на Windows) |
| 2 | Взять проект с GitHub — через git или архивом, что делать при 404 |
| 3 | Подготовка: чистое состояние, запись сессии в файл |
| 4 | **A** — установка зависимостей, нативный бинарник, первая настройка, диагностика |
| 5 | **B** — локальная модель. Главный раздел |
| 6 | **C** — подключить ИИ-клиент и убедиться, что он видит сервер |
| 7 | **D** — реальная задача через клиента |
| 8 | **E** — Docker (необязательно) |
| 9 | **F** — хуки Cursor (необязательно) |
| 10 | Краевые случаи своей платформы |
| 11 | Что собрать, если упало |
| 12 | Шаблон отчёта |
| 13 | Когда машина считается пройденной |

Все команды с `npm` — дословно одинаковые на любой ОС. Различается всё, что вокруг:
установка Node, подготовка, переменные окружения, просмотр файлов, замеры, установщик Docker.
Поэтому документа три.

## Чем отличаются, кроме синтаксиса

У каждой платформы свой набор краевых случаев, которые больше нигде не проверить:

- **macOS** — карантин Gatekeeper на нативных библиотеках; Node под Rosetta на Apple Silicon
  (тогда все замеры времени несравнимы); iCloud-синхронизация домашних папок; архитектура
  вытянутого Docker-образа.
- **Windows** — PowerShell 7 против встроенного 5.1; Defender на 600-мегабайтном файле;
  домашняя папка в OneDrive; `LongPathsEnabled`; синтаксис переменных окружения
  (`VAR=1 команда` не работает); монтирование `C:\…` в контейнер.
- **Linux** — glibc против musl (на Alpine готовые бинарники, по ожиданию, не заработают,
  и нужен точный текст ошибки); `ldd` по нативному модулю; rootless Docker и SELinux
  на монтировании `/models`; домашний каталог на сетевой ФС.

## Два места, где ответ известен заранее

В документах они помечены как «известно заранее» — тестировщик должен не удивиться, а записать:

1. **Путь к конфигурации VS Code** вычисляется по правилам Windows (`APPDATA`). На macOS и Linux
   `npm run clients:install`, по ожиданию, запишет его в `~/AppData/Roaming/Code/User/mcp.json` —
   папку, которой на этих системах не бывает, — и VS Code сервер не увидит. Cursor, Claude
   и Gemini используют пути, верные везде.
2. **Свободное место перед скачиванием не проверяется**, и **корпоративный прокси** загрузчик,
   скорее всего, не увидит (для второй попытки в ТЗ дана переменная `NODE_USE_ENV_PROXY=1`).

## Где мы сейчас

Прогоны 15–16.09 на трёх машинах дали главное: **локальная модель работает**. На macOS она
скачалась (651 с), прошла контрольные суммы, поднялась за 1.09 с и ответила вектором на
1024 измерения. На Linux система работает. На Windows скачивание зависало — блокер исправлен
16.09 и ждёт повторной проверки.

Те прогоны нашли десять дефектов; девять исправлены (реестр, Д-72 … Д-80), один оставлен
открытым осознанно (Д-81: нет проверки свободного места). Документы обновлены под исправленное
поведение — версия 3.

**Что до сих пор не подтверждено никем:** что ИИ-клиент действительно видит сервер (раздел C3).
На macOS агент не мог перезапустить GUI, на Windows шаг был заблокирован дефектом, на Linux не
дошли. Это последний критерий приёмки, за которым нет ни одного замера, и закрыть его может
любая из трёх машин за минуту.
